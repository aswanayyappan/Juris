# Frontend environment variables (Vite uses VITE_ prefix)
# Copy this to .env and fill in values

# Backend API URL — leave empty for dev (Vite proxy handles it)
# In production set to: https://juris-backend.onrender.com/api
VITE_API_URL=
